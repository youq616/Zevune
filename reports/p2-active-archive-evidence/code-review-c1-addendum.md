# C1 独立代码审核后续补充：不可作为阶段验收

记录任务：`/root/p2_archive_review`；记录时间：2026-09-17 08:26:49 UTC。

本补充保留原 `code-review-c1.md` 与 `code-review-c1-observation.json` 的原始字节，不重写历史结论。原代码审核为静态范围 PASS，但其未发现阻断的结论不能被扩大为完整阶段验收。随后得到的原生格式失败，以及另一独立审查发现、现已确认的测试覆盖缺口，使 C1 不满足合入条件。

## 保留身份

- Base：`6913d4ab2fda6956db37e0ceb49790a4518c2762`。
- C1 source：`c5f60ac440ac39032f8f8efefc2fbfbf6e299a90`。
- C1 tree：`10fb6d729c610aa1c935c96891c99e05f81d7dd1`。
- 原代码审核：13228 字节，SHA-256 `7571183736041b27d855d98d250d6fba3d85eb2845b83e68dd1b769cd586f571`。
- 原观察 JSON：5942 字节，SHA-256 `1ccd3f9c3b8f7c785f26e578222d18aec07b84516b0e409af4cf53edf4ee4898`。
- 原 JSON 的 clean_worktree 仅对应其 `2026-09-17T08:23:10.328331+00:00` 观察点。原 Markdown 写出后，作者已开始暂存后续格式修订；不能将早先清洁观察套用到后续工作树。

## 已确认的后续阻断

**CI 格式门槛失败。** 本任务随后读取了原生 Ubuntu job `105129273810` 的实际 rustfmt 差异与最终 `Process completed with exit code 1`，以及独立 native 审核原文。C1 尚未执行的 Rust 回归、Clippy 或真实付款场景不能记为通过，其他旧测试的历史通过也不能代替该候选门槛。

所读原始日志 `c1/job-105129273810.log` 的 SHA-256 为 `8cd0b609521e305ec52552a59c0730f1b34c6a4dbbc6193be17148305bdae54d`；native 审核原文 `c1/native-audit-rejected.md` 的 SHA-256 为 `df6370b998deb899748c40b3588b3bed5c9441ec356c0d5c7aadc16916b34aa0`。该记录给出精确 source、同 tree 的 PR synthetic checkout 和实际失败任务，不是作者推测的格式问题。

**Medium / 验收覆盖阻断：活动归档缺少交换两个既有段的直接回归。** 冻结设计的验收要求第 4 项明确包括交换拒绝。现有活动测试包含拆帧、提前轮换、错误签名及完整性修改，但并未直接交换两个已经存在、物理长度合法的活动段，再分别使用原 pin 和重算后的 pin 检查拒绝。旧 ZVPSEG01 分段备份的交换测试不能代替 ActiveArchive 的实际路径。这不是已证实的生产接受漏洞；它是对固定验收合同的覆盖缺口，本次初审未指出，由另一路独立审核提出后补充确认。

所需修正应复用已有真实多段 fixture，把两段原字节互换，分别验证原 pin 拒绝和重新计算准确 layout pin 后仍由真实重放拒绝，明确实际拒绝层，保留源与错误候选字节不变断言。无需新增证明捷径、改变真实付款负载或扩大设计范围。修正后须针对准确新提交重新独立审核并由原生运行确认。

## 后续候选与当前状态

作者已发布格式修订 C2：source `7477d9e0656ed751f525a4b6fe9f6649b995ede6`，tree `8e6544a6de9a8337926f70f8a89aa36e59ab7e39`，parent 为 C1。本人已看到准确本地身份及格式差异，但未签发 C2 正式 PASS；新的交换测试修正仍待下一准确候选。

当前阶段验收保持不通过／待修正，不合入 C1 或将 C2 当作已验收。下一候选需同时关闭格式门槛、交换测试覆盖和所有原生 CI 条件，并取得准确提交的非作者独立审核。
