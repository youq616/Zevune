# M1 本地存储验证

日期：2026-09-10。对象：可选本地日志、重启重放及文件锁；不是隐私链、生产数据库或安全认证。

## 本执行环境已完成

- 工具链：Go 1.23.2 linux/amd64；仅记录实际测试环境。
- 72 个顶层 Test 函数通过，0 失败；其中包含 1 个跨进程测试辅助入口。
- go vet、gofmt 和 go test -race 通过。
- 日志区块解码短时 fuzz：3 秒配置，131,814 次执行，无失败。
- 原交易解码短时 fuzz：3 秒配置，15,827 次执行，无失败。
- Linux 节点构建及真实 HTTP 启停/重启检查通过；状态摘要一致，storage.recovered 从 false 变为 true。
- Windows amd64 节点及 ledger 测试程序交叉编译通过；本执行环境没有运行 Windows 程序。

## 覆盖的行为

正常恢复、跨根窗口重放、重启后重复花费拒绝、重新校验证明、错误链与初始配置拒绝、整块失败不写入、短写与同步失败、容量上限、空文件/截断/校验损坏/多余尾部拒绝、同进程与跨进程排他锁、未调用 Close 的子进程退出后恢复，以及关闭与并发行为。

测试中能通过验证的合成数据只存在于测试文件。可执行程序仍使用 UnavailableVerifier，无支付、自动出块、钱包或最终确认能力。

## 不代表的结论

本地检查不冒充 GitHub CI。远程测试结果须查看对应提交的 Actions 运行；旧提交的成功结果不能认证本次改动。本报告不宣称断电零丢失、硬件持久化认证、存储防恶意回滚、匿名性或真实转账速度。六份先前未发布技术文档不在本次上传范围内。
